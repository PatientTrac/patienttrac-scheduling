# Publishing to GitHub Packages — @patienttrac scope
# NODE_AUTH_TOKEN must be set in CI (Netlify env var) with read:packages permission.
# Never commit a token here — use ${NODE_AUTH_TOKEN} interpolation only.
@patienttrac:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
